{
  "name": "integrity-checker-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for file integrity checker",
  "scripts": {
    "chain": "hardhat node",
    "deploy": "hardhat run scripts/deploy.ts --network localhost",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4",
    "typescript": "^5.3.3"
  }
}
